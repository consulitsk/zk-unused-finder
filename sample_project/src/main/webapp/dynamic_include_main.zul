<window viewModel="@id('vm') @init('com.example.DynamicIncludeViewModel')">
    <!-- This include has a dynamic part. The script should find all files
         named 'real_include.zul' and parse them. -->
    <include src="${vm.path}/real_include.zul" />
</window>
