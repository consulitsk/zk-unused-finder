<window viewModel="@id('vm') @init('com.example.ParentViewModel')">
    <vbox>
        <label value="@load(vm.parentMessage)" />

        <!-- This included ZUL will use the parent's ViewModel -->
        <include src="/included.zul" />
    </vbox>
</window>
