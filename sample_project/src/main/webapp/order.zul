<window viewModel="@id('vm') @init('com.example.OrderViewModel')">
    <vbox>
        <label value="Order ID:" />
        <textbox value="@bind(vm.orderId)" />
        <button label="Submit Order" onClick="@command('submitOrder')" />
        <button label="Refresh All Orders" onClick="@global-command('refreshOrders')" />
    </vbox>
</window>
